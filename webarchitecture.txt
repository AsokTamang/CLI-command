IP - internet protocal is the set of rules or protocals for transmitting(addressing,routing and delivering) the datas so called packets between the computers or clients across the internet or network
TCP - Transmission control protocal is the set of rules which make sure that the transmitted packets of data across the internet doesnot get lost


//for opening the dev tools , the short cut key is ctrl+shift+I

//ping gives us the time taken and the number of data packets travelled between the client and the server
//traceroute [domainname] for linux and tracert [domainname] for windows is used for tracing the path of the data packets between the client and server which checks about any issue in the path. 

//inorder to fetch the http header we use the code - curl -I https://www.google.com


//ipconfig is the command that lets us know the network interface of our computers , showing the ipaddresses


//ncat -vz google.com 443 command is used for checking whether the google network is open at port 443 or not

//sysdm.cpl helps us to go to the advanced settings of our pc, so that we can add the downloaded files or folders in our system path which is found under the system varibles.